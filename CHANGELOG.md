# 변경 로그

기반: [hgmzhn/manga-translator-ui](https://github.com/hgmzhn/manga-translator-ui) (`9c16097`, v3.0.0 계열)  
대상: 한국어 만화 편집·번역 워크플로

---

## 0.8 — 2026-08-27

### 작업 모드

- **자동 번역**: API로 감지·OCR·번역·렌더까지 한 번에 진행
- **TXT 내보낸 후 번역 대기 A**: 원문을 토큰 TXT로 내보낸 뒤 외부 번역
- **TXT 가져온 후 번역 완료 B**: 번역된 TXT를 JSON에 적용한 뒤 렌더
- **다시 렌더링**: 기존 JSON·인페인트로 이미지만 다시 그림 (TXT 재적용 없음)
- **클리닝만**: 글자 지움만 수행
- **번역 덧씌우기**: 이미 번역된 결과 위에 다시 렌더
- **JSON만 번역**
- **NovelAI 모드**: 이미지 글자를 텍스트 레이어로 변환. 언어 필터 없이 OCR 후 번역 없이 조판. 페이지마다 우세 언어를 감지해 줄바꿈 규칙을 고름. OCR / 보조 OCR / PaddleOCR-VL 언어 힌트를 이 모드에서만 오버라이드 가능
- 입력·출력 폴더가 같으면 작업을 시작하지 않음
- 번역 완료본을 목록에 넣으면 안내를 띄움
- 파일 목록 표시 순서를 실제 작업 순서와 맞춤
- 덮어쓰기 대화상자를 모드별로 단순화 (TXT 내보내기, 다시 렌더링)

### TXT 토큰 워크플로

- 대사 ID를 `[P0001T0001R]` 형식으로 부여. JSON에는 대괄호 없이 저장
- 페이지 번호는 파일명이 아니라 **폴더마다 0부터** 증가
- `combined script.txt`로 한 파일에 모을 수 있음 (기본 켜짐)
- 폴더 하나가 끝날 때마다 백업 TXT를 만들고 해당 폴더를 연 뒤 다음 폴더로 진행
- 언어별 도움말 텍스트를 폴더에 함께 생성
- 매칭 실패·중복 ID는 작업을 멈추지 않고 건너뜀
- TXT 내보내기는 가로 조판으로 기록. 에디터에서 세로로 고친 레이어는 세로로 저장·렌더

### 한글 조판

- **가로 비율**: 글자·자간을 좌우로 압축. 테두리는 압축된 글자 기준으로 그림
- **기울임**: 레이어 단위 기울이기 (최대 약 30°)
- 세로 원문 → 가로 번역일 때 말풍선 실제 크기를 줄바꿈 기준으로 사용
- 행두·행미 금칙 (`)」”` 등이 줄 맨 앞에 오지 않게)
- 한국어는 단어 단위로 줄바꿈. 단어가 말풍선보다 넓을 때만 글자 분할
- 원문이 한 줄이어도 번역이 안 들어가면 여러 줄로 넘김
- 사용자가 잡은 흰 박스는 유지하고 글자 크기만 맞춤. 글자 크기를 직접 바꾸면 박스도 같이 스케일
- 텍스트 박스 크기 조절 피벗을 중심으로 변경

### 편집기

**단축키** (편집기 현재 바인딩)

마우스

| 키 | 동작 |
|---|---|
| 휠 | 화면 확대/축소 |
| 가운데 클릭 | 화면 이동 |
| `Ctrl+휠` | 선택한 상자 글꼴 크기 조절 |
| `Shift+휠` | 브러시 크기 조절 (25 이하는 5 단위, 이후 20 단위) |
| `Alt+휠` | 브러시 크기 ±2 |
| `Alt+클릭` | 스포이드 (선택 도구: 글자색 / 페인트: 브러시색) |
| `Alt+Shift+클릭` | 테두리색 스포이드 (선택 도구) |
| `Ctrl+클릭` | 덧칠 브러시: 인페인트 영역을 현재 색 덧칠로 변환. 덧칠/인페인트 지우개: 클릭한 영역 삭제 |
| `Shift`+드래그 | 직선 |

도구 · 이동

| 키 | 동작 |
|---|---|
| `Q` | 선택 도구 |
| `W` | 인페인트 브러시 |
| `E` | 인페인트 지우개 |
| `R` | 덧칠 브러시 |
| `T` | 덧칠 지우개 |
| `A` / `PageUp` | 이전 이미지 |
| `D` / `PageDown` | 다음 이미지 |
| `Shift+A` / `Shift+PageUp` | 10장 이전 |
| `Shift+D` / `Shift+PageDown` | 10장 다음 |
| `F` | 창에 맞추기 |

표시

| 키 | 동작 |
|---|---|
| `F1` | 텍스트와 상자 표시 |
| `F2` | 텍스트만 표시 |
| `F3` | 상자만 표시 |
| `F4` | 아무것도 표시 안 함 |
| `F5` | 원본과 비교 (2단) |
| `Tab` | 인페인트 영역 표시 전환 |
| `Shift+Tab` | 덧칠 영역 표시 전환 |
| `Z` | 원본만 보기 전환 (페이지를 넘겨도 유지) |
| `Esc` | 입력 끝내기 |

편집 · 스타일

| 키 | 동작 |
|---|---|
| `Ctrl+Z` / `Ctrl+Y` | 실행 취소 / 다시 실행 |
| `Ctrl+C` | 선택한 텍스트 상자 복사 |
| `Ctrl+V` | 선택한 상자에 스타일 붙여넣기. 선택이 없으면 새 상자 붙여넣기 |
| `Ctrl+A` | 모든 텍스트 상자 선택 |
| `Delete` | 선택한 텍스트 상자 삭제 |
| `Ctrl+S` | 저장 |
| `1`–`0` | 해당 번호 스타일 슬롯 적용 |
| `Ctrl+1`–`0` | 선택한 상자 스타일을 해당 번호 슬롯에 복사 |
| `X` | 가로/세로 방향 전환 |
| `I` / `B` | 기울임 / 굵게 |
| `Ctrl+←` / `Ctrl+→` | 자간 |
| `Ctrl+Alt+←` / `Ctrl+Alt+→` | 가로 비율 |
| `Ctrl+↑` / `Ctrl+↓` | 줄 간격 |
| `Alt+←` / `↓` / `→` | 왼쪽 / 가운데 / 오른쪽 정렬 |
| `Alt+↑` | 4점 자유 변형 |
| `Alt+Shift+↑` / `↓` | 테두리 두께 |

- 프리셋 이름이 숫자로 시작하면 해당 번호 슬롯에 자동 할당
- 메인 목록에서 연 페이지부터 A/D가 움직임
- 표시 모드가 페이지 전환 후에도 유지
- 메인으로 나갈 때도 미저장 확인. 저장 안 함을 고르면 수정 사항을 버리고 나감
- 파일은 더블클릭으로 편집기 진입, 폴더는 한 번 클릭으로 접고 펼침
- 단축키 목록 보기 팝업
- 가운데 클릭 팬: 이미지가 화면보다 작아도 가장자리 밖으로 약 100px 이동 가능
- 브러시가 이미지 가장자리를 조금 넘어가도 칠할 수 있음
- `Ctrl`을 누르고 있으면 손가락 커서. 인페인트 → 덧칠 변환 시 원본을 되돌린 뒤 안쪽으로 4px 줄어든 덧칠을 만듦
- 속성 패널: 번역문 입력칸 확대, 실제 줄바꿈, ♡/♥ 삽입, 기울임·가로 비율, 공백 문자 정리
- 슬라이더를 한 점 클릭하면 그 위치로 이동

### 저장 · 경로 · 안정성

- JSON에 마지막 출력 폴더를 기록해 다시 저장해도 결과 폴더를 잃지 않음
- 출력 폴더 고를 때 마지막으로 쓰던 위치부터 열림
- 현재 페이지 이미지가 캐시에서 닫혀 저장이 막히던 문제 수정
- JSON 로드 실패 시 기존 내용을 빈 배열로 덮어쓰지 않음
- 마스크·이미지 크기 정보가 없어도 JSON에서 복구
- 폴더를 옮긴 뒤 JSON 절대 경로를 맞추는 **폴더 경로 재설정** 탭
- 일괄 검색·수정 탭
- PNG/WebP의 NovelAI `stealth_pngcomp` 메타데이터를 클린업·최종 이미지에 유지

### 인터페이스 · 한국어

- 목표 언어 기본값 한국어. 하드코딩된 중국어 UI·오류·로그를 한국어(또는 영어)로 교체
- 모드 이름·설명을 한국어 기준으로 다시 씀
- 설정 탭 기본 배치: 일반 · 문자 인식 · 감지 · 번역 · 복원 · 조판 · 모드 관련
- 사이드바 로고 자리, 시작 알림창(JSON으로 문구·링크 수정 가능)
- 기본 창 크기 1230×768

### 기본값

- 목표 언어 `KOR`
- 최소 글자 26, 글자 배율 1.32, 테두리 0.15
- 말풍선 교차 복구 켜짐, 감지 확률 0.05
- 출력 PNG

한국어 폰트(SUIT, 마루부리, 고운돋움, KOHI배움 등)와 `dict/KR_Manga_Multi.yaml`을 포함합니다.
